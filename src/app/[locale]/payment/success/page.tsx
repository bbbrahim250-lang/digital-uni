import Link from 'next/link';
export default function PaymentSuccess({ params }: { params: { locale: string } }) { return <main className="mx-auto max-w-2xl px-4 py-20"><h1 className="text-3xl font-bold">Payment verification</h1><p className="my-5">A provider return does not by itself confirm payment. Digital-UNI will issue a receipt only after the payment provider’s verified confirmation is recorded. If you have not received a receipt, your payment remains pending.</p><Link className="font-bold text-highlight-electric" href={`/${params.locale}/payment/pending`}>Check next steps →</Link></main>; }
